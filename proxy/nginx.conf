server {
    listen 80;

    location / {
        proxy_pass http://frontend;
    }

    location /api/ {
        proxy_pass http://backend:3000/;
    }

        location /login {
        proxy_pass http://auth:4000;
    }
}
